%table
  %thead
    %th Who needs help?
    %th Who else is helping?
    %th How will you take action?
  %tbody
    - @incentives.each_with_index do |incentive, index|
      %tr
        %td.activity{ :data => {:tooltip => incentive.activity.cause.description} }
          -if incentive.activity.cause.description
            = link_to incentive.activity.cause.name
            needs
            =  incentive.activity.name
          -else
            = incentive.activity.cause.name
            needs
            =  incentive.activity.name
          .vote
            = link_to "vote up", vote_up_path(incentive.activity), :method => :post
            = link_to "vote down", vote_down_path(incentive.activity), :method => :post
        %td.incentive
          = link_to incentive.supporter.name, incentive.supporter
          will
          = incentive.name
          = incentive_blurb incentive
          .vote
            = link_to "vote up", vote_up_path(incentive), :method => :post
            = link_to "vote down", vote_down_path(incentive), :method => :post
        %td.links
          - if incentive.provider == current_actor
            this is your reward!
          - else
            = link_to "Do the work", incentive, :class => "button"
            or
            = link_to "Support the work", new_incentive_path(:activity_id => incentive.activity.id), :class => "button"
